import 'package:flutter/material.dart';

class DeviceSelector extends StatelessWidget {
  final void Function(String) onDeviceSelect;

  DeviceSelector({required this.onDeviceSelect});

  @override
  Widget build(BuildContext context) {
    // Your device selector UI and logic here.
    // Call onDeviceSelect(selectedDeviceKey) when a device is chosen.
    return Container(); // Replace with actual widget
  }
}
